//
//  Enums.swift
//  Sportly
//
//  Created by MN on 26.05.2023.
//  Copyright © 2023 Nikita Moshyn. All rights reserved.
//

import Foundation

/*
func configureSVItems(stackView: UIStackView) {
    switch cellType {
    case .logoAndText(let logo, let text):
        
        UIImage.loadImage(with: logo) { image in
            
            let imageView = UIImageView()
            imageView.layer.cornerRadius = 15
            imageView.image = image
            imageView.frame.size = CGSize(width: 30, height: 30)
            
            let leagueLabel = UILabel()
            leagueLabel.text = text
            leagueLabel.frame.size = CGSize(width: 30, height: 30)
            leagueLabel.font = .systemFont(ofSize: 20, weight: .bold)
            leagueLabel.textColor = .black
            
            let chevronImage = UIImageView()
            chevronImage.image = UIImage(systemName: "chevron.right")!
            chevronImage.frame.size = CGSize(width: 30, height: 30)
            chevronImage.contentMode = .scaleAspectFit
            
            let spacing = Sportly.getSpacing(width: 40, height: nil)
            spacing.frame.size = CGSize(width: 30, height: 30)
            
            stackView.addSubview(imageView)
            stackView.addSubview(leagueLabel)
            stackView.addSubview(spacing)
            stackView.addSubview(chevronImage)
        }
        
    case .logosAndText(let logos, let text):
        
        guard logos.count >= 1 && logos.count <= 3 else { return }
        var imageViews: [UIImageView] = []
        
        for logo in logos {
            UIImage.loadImage(with: logo) { image in
                
            }
        }
        break
    case .doubleText(let text1, let text2):
        break
    }
}
*/
